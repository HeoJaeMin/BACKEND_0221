server:
  post: 8081

spring:
  h2:
    console:
      enable: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:book
    username: sa
    password:
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: create